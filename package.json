{
  "name": "chaoslib",
  "version": "1.0.0",
  "description": "A lightweight chaos engineering toolkit for injecting controlled failures into Node.js and Python APIs",
  "private": true,
  "workspaces": [
    "packages/chaoslib-node",
    "packages/chaoslib-cli",
    "examples/node-demo-api"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
